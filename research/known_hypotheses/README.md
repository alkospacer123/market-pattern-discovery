# Known hypothesis library

Future `KH-*.json` records require: `hypothesis_id`, `source`,
`source_reference`, `human_statement`, `machine_testable_statement`,
`required_features`, `required_behavior_outputs`,
`current_feature_set_compatible`, `requires_future_feature_set_version`,
`status`, and `notes`. No Gerchik hypothesis is encoded in Phase 4A.
