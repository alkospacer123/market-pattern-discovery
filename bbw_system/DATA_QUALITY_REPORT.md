# Data quality report

No target market datasets were available, so duplicates, gaps, invalid bars, weekends, holidays, liquidity, and rollover jumps could not be measured. This is a **not measured** result, not a clean-data finding. The local runner generates per-dataset JSON, Markdown, coverage CSVs, and `liquidity_profile.csv` after explicit passport metadata is supplied.
