# Temporal alignment readiness — CNYRUBF

**Status: UNRESOLVED**

Timestamp semantics is resolved as START, but the frozen authoritative exchange calendar needed for weekday-evening and weekend trading-date assignment is missing. The extended audit must also be run against the external hash-verified bundle to emit session-boundary, clearing, and H1-edge observations.

Normalization and every downstream research stage remain prohibited. No fallback calendar rule is permitted.
