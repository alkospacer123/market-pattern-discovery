# Temporal alignment readiness — CNYRUBF

**Status: UNRESOLVED**

Timestamp semantics is resolved as START. Explicit frozen-calendar loading and fail-closed coverage checks are implemented, but an authoritative MOEX calendar artifact was not supplied. The extended audit must be run with that artifact and the external hash-verified bundle to emit weekday-evening, weekend, special-session, regime-transition, clearing, and H1-edge observations.

Normalization and every downstream research stage remain prohibited. No fallback calendar rule is permitted.
